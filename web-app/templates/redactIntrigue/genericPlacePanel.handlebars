<div class="tab-pane" id="place_{{ genericPlace.id }}">
    <form name="updatePlace_{{ genericPlace.id }}" data-url="/gnk/genericPlace/update/{{ genericPlace.id }}">
        <input type="hidden" name="id" value="{{ genericPlace.id }}"/>
        <input type="hidden" name="plotId" id="plotId" value="{{ genericPlace.plotId }}"/>

        <div style="margin:auto">
            <div class="row formRow">
                <div class="span1">
                    <label for="placeCode">
                        Nom
                    </label>
                </div>
                <div class="span4">
                    <input type="text" name="placeCode" id="placeCode" value="{{ genericPlace.code }}" required=""/>
                </div>
                <div class="span1">
                    <label>
                        Tags
                    </label>
                </div>
                <div class="span4">
                    <a href="#placeTagsModal_{{ genericPlace.id }}" class="btn" data-toggle="modal">
                        Choisir les tags
                    </a>
                </div>
            </div>
            <!--<div class="row formRow">-->
                <!--<div class="span1">-->
                    <!--<label>-->
                        <!--Scènes passées-->
                    <!--</label>-->
                <!--</div>-->
                <!--<div class="span4">-->
                    <!--<a href="#placePastScenesModal_{{ genericPlace.id }}" class="btn" data-toggle="modal">-->
                        <!--Voir les scènes passées-->
                    <!--</a>-->
                <!--</div>-->
                <!--<div class="span1">-->
                    <!--<label>-->
                       <!--Evènements-->
                    <!--</label>-->
                <!--</div>-->
                <!--<div class="span4">-->
                    <!--<a href="#placeEventsModal_{{ genericPlace.id }}" class="btn" data-toggle="modal">-->
                        <!--Voir les évènements-->
                    <!--</a>-->
                <!--</div>-->
            <!--</div>-->
            <div class="row formRow text-center">
                <label for="placeDescription">
                    Description
                </label>
            </div>
            <textarea name="placeDescription" id="placeDescription" rows="5" cols="100">{{{ genericPlace.comment }}}</textarea>
        </div>
        <!--<div id="placeEventsModal_{{ genericPlace.id }}" class="modal hide fade largeModal" tabindex="-1">-->
            <!--<div class="modal-header">-->
                <!--<button type="button" class="close" data-dismiss="modal">×</button>-->
                <!--<h3>-->
                    <!--Evènements-->
                <!--</h3>-->
                <!--<input class="input-medium search-query" data-content="placeEvent{{ genericPlace.id }}"-->
                       <!--placeholder="Rechercher..."/>-->
            <!--</div>-->

            <!--<div class="modal-body">-->
                <!--<ul class="placeEvent{{ genericPlace.id }}">-->
                    <!--{{#each genericPlace.eventList}}-->
                        <!--<li class="modalLi" data-name="{{toLowerCase eventName }}">-->
                            <!--<label>-->
                                <!--&lt;!&ndash;<input type="checkbox" name="placeEvent_{{ eventId }}" id="placeEvent_{{ eventId }}"/>&ndash;&gt;-->
                                <!--{{ eventName }}-->
                            <!--</label>-->
                        <!--</li>-->
                    <!--{{/each}}-->
                <!--</ul>-->
            <!--</div>-->

            <!--<div class="modal-footer">-->
                <!--<button class="btn" data-dismiss="modal">Ok</button>-->
            <!--</div>-->
        <!--</div>-->
        <!--<div id="placePastScenesModal_{{ genericPlace.id }}" class="modal hide fade largeModal" tabindex="-1">-->
            <!--<div class="modal-header">-->
                <!--<button type="button" class="close" data-dismiss="modal">×</button>-->
                <!--<h3>-->
                    <!--Scènes passées-->
                <!--</h3>-->
                <!--<input class="input-medium search-query" data-content="placePastScene{{ genericPlace.id }}"-->
                       <!--placeholder="Rechercher..."/>-->
            <!--</div>-->

            <!--<div class="modal-body">-->
                <!--<ul class="placePastScene{{ genericPlace.id }}">-->
                    <!--{{#each genericPlace.pastsceneList}}-->
                        <!--<li class="modalLi" data-name="{{toLowerCase pastsceneTitle }}">-->
                            <!--<label>-->
                                <!--&lt;!&ndash;<input type="checkbox" name="placePastScene_{{ pastsceneId }}" id="placePastScene_{{ pastsceneId }}"/>&ndash;&gt;-->
                                <!--{{ pastsceneTitle }}-->
                            <!--</label>-->
                        <!--</li>-->
                    <!--{{/each}}-->
                <!--</ul>-->
            <!--</div>-->

            <!--<div class="modal-footer">-->
                <!--<button class="btn" data-dismiss="modal">Ok</button>-->
            <!--</div>-->
        <!--</div>-->

        <div id="placeTagsModal_{{ genericPlace.id }}" class="modal hide fade" tabindex="-1">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">×</button>
                <h3 id="myModalLabel{{ genericPlace.id }}">Tags</h3>
                <input class="input-medium search-query" data-content="placeTags{{ genericPlace.id }}" placeholder="Rechercher..."/>
            </div>

            <div class="modal-body">
                <ul class="placeTags{{ genericPlace.id }}">
                {{#each genericPlaceTagList}}
                    <li class="modalLi" data-name="{{toLowerCase name }}">
                        <label>
                            <input type="checkbox" name="placeTags_{{ id }}" id="placeTags_{{ id }}"/>
                            {{ name }}
                        </label>
                    </li>
                {{/each}}
                </ul>
            </div>

            <div class="modal-footer">
                <button class="btn" data-dismiss="modal">Ok</button>
            </div>
        </div>
        <input type="button" name="Update" data-id="{{ genericPlace.id }}" value="Update" class="btn btn-primary updatePlace"/>
    </form>
</div>
